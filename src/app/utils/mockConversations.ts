// Deprecated: Mock data removed for production
// Keep empty exports to avoid breaking imports

export const mockConversations = [];
export const mockRequests = [];
export const mockActivityPolls = [];