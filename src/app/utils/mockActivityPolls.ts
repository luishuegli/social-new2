import { ActivityPoll } from '../types';

// Deprecated: Mock data removed for production
// Keep empty export to avoid breaking imports
export const mockActivityPolls: (ActivityPoll & { userVoted?: boolean })[] = [];